import { Text, View } from 'native-base'

export default function PermitInfo({ navigation }: { navigation: any }) {
  return (
    <View flex={1} justifyContent='center' alignItems='center'>
      <Text fontSize='xl'>PDF Placeholder</Text>
    </View>
  )
}
